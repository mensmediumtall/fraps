.js-modal-form-components.css-modal-form-mask(style="display:none;")
.js-modal-form-components.css-modal-form-container(style="display:none;")
  .css-modal-form
    h2 Thanks!
    p.css-short-desc.
      Rest assured we are working on the problem.
      If you'd like, you can help by filling out this form
      so we can find the perfect size.
    p.
      Give us a like?
    .fb-like(
        data-href="http://fb.com/mensmediumtall"
      , data-send="true"
      , data-layout="button_count"
      , data-width="150"
      , data-show-faces="false"
      , data-font="verdana"
      , data-colorscheme="light"
    )
    form#js-optional-info
      .css-field
        p.css-title
          label Height
        input(type="number", min="5", max="8", step="any", name="feet", value="6", required="required")
        span.
          ft
        input(type="number", min="0", max="12", step="any", name="inches", required="required")
        span.
          in

        p.css-title
          label Pant Waist
        input(type="number", min="10", step="any", name="waist", required="required")
        span.
          in

        p.css-title
          label Pant Length (inseam)
        input(type="number", min="10", step="any", name="length", required="required")
        span.
          in

        p.css-title
          label Weight
        input(type="number", min="50", max="350", step="any", name="weight", required="required")
        span.
          lbs
      button.css-big-a-button(type="submit") Submit
    br
    .css-fb-big-like
      include fb-like
    //-
      .fb-like(
          data-href="http://fb.com/mensmediumtall"
        , data-send="true"
        , data-layout="button_count"
        , data-width="150"
        , data-show-faces="false"
        , data-font="verdana"
        , data-colorscheme="light"
      )
